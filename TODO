* Ajouter la prise en charge des commentaires (#) dans les fichiers .keep et
  .ignore

* Ajouter la possibilité d'exécuter les makefiles dans les répertoires s'il y
  en a (cf Makefile du noyau linux)

* Fixer les BUGS (grep FIXME *)